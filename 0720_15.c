#include <stdio.h>

int main()
{
    int a = 10, b = 5;
    int r1, r2, r3;

    r1=a>b;
    r2=a<10;
    r3=(a>b) && (a<10);

    if ((a>b) && (a<10) );
    printf("(a>b) && (a<10) 식은 참입니다. : ", (a>b) && (a<10));

}